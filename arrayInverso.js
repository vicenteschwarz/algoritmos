let lista = [10, 20, 30, 40, 50]
let listaInversa = []   

for(let i =lista.length -1; i>=0; i--){
listaInversa.push(lista[i])
}
console.log("Os números da array, organizados de forma inversa, são", listaInversa)